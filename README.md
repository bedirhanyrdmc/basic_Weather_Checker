# basic_Weather_Checker
A simple Python script that fetches the current weather for any city using the OpenWeather API.
It shows the weather description and temperature in Fahrenheit.

Add your OpenWeather API key inside the script
